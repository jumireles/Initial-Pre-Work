//Learned how to use the strict inequality operator that will only retun true if
//the values being compared are the same number and the same type such as number and number not string and number.
// Setup
function testStrictNotEqual(val) {
  // Only Change Code Below this Line

  if (val!== 17) {

  // Only Change Code Above this Line

    return "Not Equal";
  }
  return "Equal";
}

// Change this value to test
testStrictNotEqual(10);
