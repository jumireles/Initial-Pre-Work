//Learned that you can create a function with multiple parameters and that you can
//pass along arguments to those parameters when you call the function with the
//parameters that you wish to input such as additionProblem(2,2);
// Example
function ourFunctionWithArgs(a, b) {
  console.log(a - b);
}
ourFunctionWithArgs(10, 5); // Outputs 5

// Only change code below this line.

function functionWithArgs(num1, num2){
  console.log(num1 + num2)
}
functionWithArgs(1,2);
