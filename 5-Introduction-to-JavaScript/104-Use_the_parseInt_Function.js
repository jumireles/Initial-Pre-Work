//Learned how to change a string number into an integer using the parseInt function.
//Also if string is not a number parseInt return NaN.
function convertToInteger(str) {

 var convo = parseInt(str);
 return convo;

}

convertToInteger("56");
