//Learned how to use muliple condtional tenarary's which can subistute multiple if statements.
function checkSign(num) {
  return (num > 0) ? "positive" : (num < 0) ? "negative" : "zero";

}

checkSign(10);
