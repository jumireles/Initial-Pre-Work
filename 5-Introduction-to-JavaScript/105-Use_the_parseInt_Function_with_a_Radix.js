//Learned how to use radix in a parseInt which helps convert a binary number into an integer.
function convertToInteger(str) {

  var convo = parseInt(str, 2);
  return convo;

}

convertToInteger("10011");
