//Learned that you can use eqaulity operators to return boolean values (true or false)
//from functions instead of having to use many if else statements.
function isLess(a, b) {
  // Fix this code
 return a < b;

}

// Change these values to test
isLess(10, 15);
